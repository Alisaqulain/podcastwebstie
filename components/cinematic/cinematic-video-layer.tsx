/**
 * Semantic alias — cinematic stack uses AmbientVideoBackdrop + overlays.
 * Prefer importing from `@/components/cinematic` or use AmbientVideoBackdrop directly.
 */
export { AmbientVideoBackdrop as CinematicVideoLayer } from "./ambient-video-backdrop";
